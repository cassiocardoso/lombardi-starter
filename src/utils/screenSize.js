// The values for the breakpoints come from Tailwind basic config

export const isSmallScreen = screenSize => screenSize <= 640

export const isMediumScreen = screenSize => screenSize <= 768

export const isLargeScreen = screenSize => screenSize <= 1024

export const isExtraLargeScreen = screenSize => screenSize <= 1280
